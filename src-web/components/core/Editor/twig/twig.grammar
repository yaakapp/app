@top Template { (Tag | Text)* }

@local tokens {
  Close { "]}" }
  @else Content
}

@skip { } {
  Open { "${[" }
  Tag { Open (Content)+ Close }
}

@tokens {
  Text { ![$] Text? }
}

@external propSource highlight from "./highlight"
